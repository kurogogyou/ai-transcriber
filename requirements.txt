# AI Transcriber - Python Dependencies
#
# Install with:
#   pip install torch==2.4.0+cu118 torchvision==0.19.0+cu118 torchaudio==2.4.0+cu118 --index-url https://download.pytorch.org/whl/cu118
#   pip install -r requirements.txt
#
# PyTorch must be installed first from the cu118 index to ensure matching versions.
# CUDA 11.8 is required for GTX 1070 (compute capability 6.1) support.
# For CPU-only, skip the first command and just run: pip install -r requirements.txt

# whisper-ctranslate2 (fast CTranslate2-based transcription, drop-in whisper CLI replacement)
whisper-ctranslate2

# WhisperX (for speaker diarization)
whisperx

# Pin PyTorch versions to prevent whisperx from upgrading them
torch==2.4.0+cu118
torchvision==0.19.0+cu118
torchaudio==2.4.0+cu118

# FFmpeg is also required (system package):
#   sudo apt install ffmpeg

# Speaker Diarization Setup:
#   1. Follow install instructions above
#   2. Get HuggingFace token from https://huggingface.co/settings/tokens
#   3. Accept pyannote license at https://huggingface.co/pyannote/speaker-diarization-3.1
#   4. Export HF_TOKEN=your_token_here

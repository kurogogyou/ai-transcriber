# Post-Phase-4 (2026-06-14): only the gpu-broker client + requests.
# No torch / whisperx / pyannote / cuDNN8 — all of that lives in the
# broker-managed whisperx-server:0.1.0 container.
requests>=2.31
